# Spore-Mod-Installer
A bash-based mod installer for Spore on Linux. Parses ModInfo.xml from .sporemod bundles to present component descriptions, enforce group selections, and auto-install prerequisite DLLs via SporeModLoader.
